# Feature: {feature_name}

Date: {date}
Status: Draft
Type: background
Source: {source}

## Overview

{overview}

## User Stories

- As a {user_type}, I want to {action}, so that {benefit}

## Acceptance Criteria

- [ ] {criterion_1}
- [ ] {criterion_2}

## Workers

### {worker_name}

- **Type**: {OneTimeWorkRequest/PeriodicWorkRequest/Service/BroadcastReceiver}
- **Class**: `{fully_qualified_class_name}`
- **Purpose**: {purpose}

#### Scheduling

| Property | Value |
|----------|-------|
| Trigger | {event/schedule/manual} |
| Schedule | {cron_expression_or_interval} |
| Initial Delay | {duration} |
| Flex Interval | {duration_or_na} |
| Backoff Policy | {LINEAR/EXPONENTIAL} |
| Backoff Delay | {duration} |

#### Constraints

| Constraint | Required |
|------------|----------|
| Network | {CONNECTED/UNMETERED/NOT_ROAMING/NOT_REQUIRED} |
| Battery | {NOT_LOW/any} |
| Storage | {NOT_LOW/any} |
| Charging | {Yes/No} |
| Idle | {Yes/No} |

#### Input / Output

**Input Data**:
| Key | Type | Required | Description |
|-----|------|----------|-------------|
| {key} | {type} | Yes/No | {description} |

**Output Data**:
| Key | Type | Description |
|-----|------|-------------|
| {key} | {type} | {description} |

## Error Handling & Retry

| Error Type | Action | Max Retries | Backoff |
|------------|--------|-------------|---------|
| Network failure | Retry | {count} | {strategy} |
| Auth expired | Retry after refresh | {count} | {strategy} |
| Data corruption | Fail + report | 0 | N/A |
| Unknown | Retry then fail | {count} | {strategy} |

### Failure Escalation

1. After max retries: {action — e.g., notify user, log to analytics, schedule later}
2. On persistent failure: {action — e.g., disable feature, alert backend}

## Resource Management

| Resource | Acquisition | Release | Timeout |
|----------|-------------|---------|---------|
| {wake_lock/db_connection/network} | {when} | {when} | {duration} |

## Monitoring & Observability

### Logging

| Event | Level | Data |
|-------|-------|------|
| Worker started | INFO | {worker_name, input_data} |
| Worker completed | INFO | {worker_name, duration, output_data} |
| Worker failed | ERROR | {worker_name, error, attempt_count} |
| Worker retrying | WARN | {worker_name, error, next_attempt} |

### Analytics Events

| Event | Trigger | Properties |
|-------|---------|------------|
| {event_name} | {trigger} | {properties} |

### Health Checks

| Check | Frequency | Alert Threshold |
|-------|-----------|-----------------|
| {check_name} | {interval} | {condition} |

## Data Flow

```
{trigger} --> {worker_1} --> {storage/api} --> {worker_2} --> {result}
```

## Concurrency

| Scenario | Policy |
|----------|--------|
| Duplicate work request | {KEEP/REPLACE/APPEND} |
| Concurrent execution | {allowed/serialized/mutex} |
| App killed during execution | {resume/restart/abandon} |

## Testing Strategy

| Test Type | Scope | How |
|-----------|-------|-----|
| Unit | Worker logic | {approach} |
| Integration | Worker + dependencies | {approach} |
| Timing | Schedule accuracy | {approach} |
| Failure | Retry behavior | {approach} |

## Technical Notes

{technical_notes}

## Dependencies

{dependencies}

## Out of Scope

{out_of_scope}
